//
//  0.5_intro.compiler.linker.and.cpp
//  Chapter.0
//
//  Created by Judah's Leon on 7/13/22.
//

/*
 libraries : precompiled code for reuse(packaged up)
 standard library : provides additional functionality to use in programs
 
 compiling: check syntax > aborts compilation if error is not fixed
 >  object file(with .o extension)
 
 > linking object files and libraries(combines all object files into single
 executable file, ex : exe
 > linker links also library files + standard library(ex: input output)
 
 > linker makes sure all cross-file dependencies are resolved properly
 If the linker is unable to connect a reference to something with its
 definition, you’ll get a linker error, and the linking process will abort.
 
 > executable file which user can run
 
 
 
 
 testing and debugging : to figure out what's wrong with the program
 
 IDEs : software that bundles editor, compiler, linker, debugger
 complete package for development just with one software
 */
